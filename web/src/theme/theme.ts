import {extendTheme} from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    bg: {
      gray: '#F5F5F5',
      white: '#FFF',
    },
    text: {
      black: '#404040',
      blue: '#0F4264',
      gray: '#A09F9F',
    },
  },
});

export default theme;
